# ProvGate Backend Dependencies
# 설치 방법: pip install -r requirements.txt

# ================================================================
# 웹 프레임워크
# ================================================================
fastapi==0.135.2          # FastAPI 웹 프레임워크
uvicorn==0.42.0           # ASGI 서버 (FastAPI 실행용)
starlette==1.0.0          # FastAPI 내부 의존성

# ================================================================
# 데이터베이스
# ================================================================
SQLAlchemy==2.0.48        # ORM (비동기 쿼리 지원)
asyncpg==0.31.0           # PostgreSQL 비동기 드라이버
greenlet==3.3.2           # SQLAlchemy 비동기 지원

# ================================================================
# 데이터 검증
# ================================================================
pydantic==2.12.5          # 데이터 검증 라이브러리
pydantic-settings==2.13.1 # 환경변수 관리 (.env 파일 로드)
pydantic_core==2.41.5     # Pydantic 내부 의존성
annotated-types==0.7.0    # Pydantic 타입 어노테이션 지원

# ================================================================
# AI
# ================================================================
anthropic==0.86.0         # Claude API 클라이언트 (힌트/게이트/진단 생성)

# ================================================================
# Supabase (인증 + DB)
# ================================================================
supabase==2.30.0          # Supabase Python SDK (JWT 검증)
supabase-auth==2.30.0     # Supabase Auth 클라이언트
supabase-functions==2.30.0 # Supabase Functions 클라이언트
postgrest==2.30.0         # Supabase PostgREST 클라이언트
realtime==2.30.0          # Supabase Realtime 클라이언트
storage3==2.30.0          # Supabase Storage 클라이언트

# ================================================================
# HTTP 클라이언트
# ================================================================
httpx==0.28.1             # 비동기 HTTP 클라이언트
httpcore==1.0.9           # httpx 내부 의존성
h11==0.16.0               # HTTP/1.1 프로토콜 구현
h2==4.3.0                 # HTTP/2 프로토콜 구현
hpack==4.1.0              # HTTP/2 헤더 압축
hyperframe==6.1.0         # HTTP/2 프레임 처리

# ================================================================
# 환경변수 / 설정
# ================================================================
python-dotenv==1.2.2      # .env 파일 로드

# ================================================================
# YAML (문제 파일 관리)
# ================================================================
PyYAML==6.0.3             # YAML 파일 파싱 (sync_problems.py)

# ================================================================
# 유틸리티
# ================================================================
anyio==4.13.0             # 비동기 유틸리티
sniffio==1.3.1            # 비동기 라이브러리 감지
typing_extensions==4.15.0 # 타입 힌트 확장
python-dateutil==2.9.0.post0 # 날짜/시간 처리
certifi==2026.2.25        # SSL 인증서
charset-normalizer==3.4.7 # 문자 인코딩 감지
idna==3.11                # 국제화 도메인 처리
urllib3==2.7.0            # HTTP 클라이언트 유틸리티
requests==2.34.2          # HTTP 요청 (Supabase SDK 내부 사용)
websockets==15.0.1        # WebSocket 지원 (Supabase Realtime)
packaging==24.0           # 패키지 버전 관리

# ================================================================
# 보안
# ================================================================
cryptography==48.0.0      # 암호화 라이브러리
PyJWT==2.13.0             # JWT 토큰 처리